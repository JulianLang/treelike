{
  "name": "treelike",
  "version": "0.0.32",
  "description": "A library to convert objects into a tree-like structure.",
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "files": [
    "dist/src/**/*"
  ],
  "scripts": {
    "dev": "npx tsc -p . -w",
    "build": "npx rimraf ./dist && npx tsc -p .",
    "test": "npx jasmine-ts --config=\"jasmine.json\"",
    "test:watch": "nodemon --quiet --ext ts --exec 'npx jasmine-ts --config=\"jasmine.json\"'",
    "start": "npx ts-node ./playground.ts"
  },
  "author": "jlang",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^26.0.14",
    "@types/node": "^12.7.4",
    "jasmine-ts": "^0.3.0",
    "jest": "^26.4.2",
    "nodemon": "^1.19.4",
    "rimraf": "^3.0.1",
    "ts-node": "^9.0.0",
    "typescript": "^4.0.2"
  }
}
